name: Check formatting
on:
  merge_group:
  push:
    branches:
      - 'main'
      - 'stable-*'
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Clone repository
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Set up Javascript environment
        uses: ./.github/actions/setup-javascript

      - name: Show formatter diff for verified badge
        run: |
          yarn oxfmt app/javascript/mastodon/components/display_name/verified_badge.tsx app/javascript/mastodon/components/display_name/verified_badge.module.scss
          git diff -- app/javascript/mastodon/components/display_name/verified_badge.tsx app/javascript/mastodon/components/display_name/verified_badge.module.scss
          exit 1

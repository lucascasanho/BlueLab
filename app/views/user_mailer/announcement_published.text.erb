<%= t('user_mailer.announcement_published.title', domain: site_hostname) %>

===

<%= t('user_mailer.announcement_published.description', domain: site_hostname) %>

<%= @announcement.text %>
<% if @announcement.media_attachments.any? %>

<%= t('media_attachments.attachments', count: @announcement.media_attachments.size, default: 'Media') %>:
<% @announcement.media_attachments.each do |media| %>
- <%= media.description.presence || t('media_attachments.attachments', count: 1, default: 'Media') %>: <%= full_asset_url(media.file.url(:original)) %>
<% end %>
<% end %>

<%= t('user_mailer.announcement_published.title') %>

===

<%= t('user_mailer.announcement_published.description', domain: site_hostname) %>

<%= @announcement.text %>
<% if @announcement.media_attachments.any? %>

<%= t('media_attachments.attachments', count: @announcement.media_attachments.size, default: 'Mídia') %>:
<% @announcement.media_attachments.each do |media| %>
- <%= media.description.presence || media.file_file_name || 'Mídia' %>: <%= full_asset_url(media.file.url(:original)) %>
<% end %>
<% end %>
